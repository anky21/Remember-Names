<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:focusable="true"
    android:focusableInTouchMode="true"
    tools:context="me.anky.connectid.edit.EditActivity">

    <ImageView
        android:id="@+id/edit_portrait_iv"
        android:layout_marginBottom="@dimen/margin_16dp"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:src="@drawable/blank_profile_picture"
        android:scaleType="centerCrop"/>

    <EditText
        android:id="@+id/name_et"
        android:textSize="24sp"
        android:hint="Name..."
        android:nextFocusUp="@id/name_et"
        android:nextFocusLeft="@id/name_et"
        android:layout_width="200dp"
        android:layout_height="wrap_content" />

    <EditText
        android:id="@+id/description_et"
        android:textSize="24sp"
        android:hint="Description..."
        android:layout_width="200dp"
        android:layout_height="wrap_content" />

    <Button
        android:text="Add Connection"
        android:onClick="handleAddConnectionClicked"
        android:layout_marginTop="40dp"
        android:textSize="24sp"
        android:gravity="center"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

</LinearLayout>
